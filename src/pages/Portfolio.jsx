import React from "react";
import styles from "./about.module.css";

const Portfolio = () => {
	return (
		<div className={styles.pageContainer} id="portfolio">
			Portfolio
		</div>
	);
};

export default Portfolio;
